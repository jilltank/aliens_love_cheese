<div class="row">
	<div class="col-md-4">
		<div class="center">
			<h2>The Nearest Spaceship to <%= "#{@location.city}" %></h2>
			<p>pssst! This might be a fun story, but it's based on real science! <a data-toggle="modal" data-target=".bs-example-modal-lg">Find out more</a></p>
			<a data-toggle="popover" title="Fun Fact" data-content="<%= @fun_facts[0] %>"><%= image_tag "alien.png", id: "strings", class: "img-responsive" %></a>
			<%= link_to "Check Somewhere Else", winner_path, class: "btn btn-success" %>
		</div>
	</div>
	<div class="col-md-8">
		<div id="map">
		</div>
	</div>
</div>

<div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
    	<div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">x</span></button>
      </div>
    	<div class="container">
      	<h2 class="center-it">NASA and Meteorites</h2>
      	<br>
    		<div class="row">
    			<div class="col-md-8">
    				<p>The 'spaceship' crash sites used on this site are the actual sites where scientists have discovered and documented real meteorites, which are pieces of larger space rocks that crashed to the Earth. The data is taken from NASA's database of over 45,716 meteorite finds. Watch the video below to find out more about meteorites.</p>
    			</div>
    			<div class="col-md-4">
    				<%= image_tag "meteorite.png", width: "100" %>
    			</div>
    		</div>
    		<br>
    		<iframe width="560" height="315" src="https://www.youtube.com/embed/02wrLS-ue1Q" frameborder="0" allowfullscreen></iframe>
    		<br><br>
    	</div>
    </div>
  </div>
</div>


	<script type="text/javascript">

	$(document).ready(function (){
		function initialize () {

			var mapOptions = {
				center: {lat: <%= @nacho_force_one["latitude"] %>, lng: <%= @nacho_force_one["longitude"] %>},
				zoom: 8,
				scrollwheel: false
			};

			var map = new google.maps.Map(document.getElementById('map'), mapOptions);

			var image = {
				url: "<%= asset_path 'spaceship2.png' %>",
			}

			var image2 = {
				url: "<%= asset_path 'nachos3.png' %>",
			}

			var marker = new google.maps.Marker({
				position: {lat: <%= @nearby_ship.lat %>, lng: <%= @nearby_ship.long %>},
				map: map,
				title: "<%= @nearby_ship.name %>",
				icon: image,
				animation: google.maps.Animation.BOUNCE
			});

			var marker2 = new google.maps.Marker({
				position: {lat: <%= @nacho_force_one["latitude"].to_f %>, lng: <%= @nacho_force_one["longitude"].to_f %>},
				map: map,
				title: "<%= @nacho_force_one["name"] %>",
				icon: image2,
				animation: google.maps.Animation.BOUNCE
			});

			var contentString = "<h3>Landing Site: <%= @nearby_ship.name %></h3><p>GPS coordinates: <%= @nearby_ship.lat %>, <%= @nearby_ship.long %></p>";
			var contentString2 = "<h3><%= @nacho_force_one["name"] %></h3><p><%= @nacho_force_one["address"] %></p><p><%= @nacho_force_one["city"] %>, <%= @nacho_force_one["state"] %></p>";
			var infowindow = new google.maps.InfoWindow({content: contentString});
			var infowindow2 = new google.maps.InfoWindow({content: contentString2});
			google.maps.event.addListener(marker, 'click', function() {
				infowindow.open(map, marker);
			});
			google.maps.event.addListener(marker2, 'click', function() {
				infowindow2.open(map, marker2);
			});

		}

		google.maps.event.addDomListener(window, 'load', initialize);
	});

	</script>
